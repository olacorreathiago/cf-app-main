import type { Metadata } from "next";

export const metadata: Metadata = { title: "Boxes" };

export default function BoxesDirectoryPage() {
  return (
    <main className="p-4">
      <p className="text-white/60">Directório de boxes — Fase 2</p>
    </main>
  );
}
